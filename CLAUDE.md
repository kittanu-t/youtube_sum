# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working in this repository.

## Project

`youtube_sum` — a project for summarizing YouTube videos. The repository is currently empty (no source code or dependencies yet). When building this project, establish clear patterns and document them here.

## Notes

- This is a greenfield project. There are no existing build scripts, linting rules, or test frameworks configured.
- The project is at `C:\Users\User\youtube_sum` on Windows.
- The `.claude/settings.local.json` pre-approves `git` commands and basic directory listings.

### When Code Exists

Once an initial project structure is established, update this file with:
- Build/run/test/lint commands
- Tech stack details
- Key architectural patterns (entry points, module organization, config conventions)
- Any settings or conventions that differ from the defaults
